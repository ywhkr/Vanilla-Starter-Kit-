//quiz #1
const str1 = 'Vanilla';
const str2 = ' ';
const str3 = 'Coding';
const result = str1 + str2 + str3;

if (result === 'Vanilla Coding') {
  alert('🎉');
}

//quiz #2
const str1 = 'Vanilla';
const str2 = 'Coding';

const length1 = str1.length;
const length2 = str2.length;

const result = length1 + length2;

if (result === 13) {
  alert('🎉');
}

//quiz #3
const str = 'Vanilla';

const result = str[0];

if (result === 'V') {
  alert('🎉');
}

//quiz #4
const str = 'Vanilla';

const result = str[str.length - 1];

if (result === 'a') {
  alert('🎉');
}

//quiz #5
const myName = 'Justin';

const lastCharacter = myName[myName.length - 1];

if (lastCharacter === 'n') {
  alert('🎉');
}
